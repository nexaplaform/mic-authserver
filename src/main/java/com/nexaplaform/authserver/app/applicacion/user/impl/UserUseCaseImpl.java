package com.nexaplaform.authserver.app.applicacion.user.impl;

import com.nexaplaform.authserver.app.applicacion.user.UserUseCase;
import com.nexaplaform.authserver.app.dominio.user.User;
import org.springframework.stereotype.Service;

@Service
public class UserUseCaseImpl implements UserUseCase {
    
    @Override
    public User create(User value) {
        return null;
    }
}
